//genral set of opeartion applied on various types of adata
//ex-if suppose we want to find the maximum values of two values 
//and then same operation is to be applied on float values
//rather than amking two funxtions we cam do it on only
#include<bits/stdc++.h>
using namespace std;
template<typename mytype> void display(mytype x){
    cout<<"you have passed"<<x<<endl;
} 
int main()
{
    display(10);
    
}